A Network Intrustion Detection System which tracks packets and informs user of suspicious activity.

I recommend using a virtual environemnt to run this tracker for yourself in its current stage
To create a Venv run the following code in your terminal (works for Bash):

python -m venv .venv
source .venv/bins/activate
python -m pip install -r requirements.txt
